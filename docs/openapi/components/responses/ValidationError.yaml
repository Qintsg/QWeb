description: 请求参数校验失败。
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    example:
      code: 400
      message: 参数错误
      errors:
        username:
          - 用户名不可用
